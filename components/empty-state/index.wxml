<view class="empty">
  <image wx:if="{{iconSrc}}" class="empty-img" src="{{iconSrc}}" mode="aspectFit" />
  <view class="empty-text">{{text}}</view>
  <view wx:if="{{sub}}" class="empty-sub">{{sub}}</view>
</view>
